const reducer1 = (state = '', action) => {
  switch (action.type) {
    default:
      return state;
  }
};

const reducer2 = (state = '', action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { reducer1, reducer2 };
